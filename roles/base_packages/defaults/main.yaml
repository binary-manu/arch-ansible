base_packages_list:
  # Arch's base packages
  - base
  - base-devel
  # Additional tools needed to run Ansible and get network connectivity
  # after we reboot
  - sudo
  - networkmanager
  - openssh
  - python3
